# sthylophone-rpi-pico
pins:
  gpio 0-12 keyboard (c4-c5)
  gpio 13 - octave button (connect to gnd)
  adc0 - vibrato with off below 10% 
  adc1 - attack with same thing(max 2000ms)
  adc2 - relase with same thing(max 2000ms)
  gpio 16 - audio (better add 100nf capilator)
